[물밑] {{ headline }}
입찰공고보다 먼저 포착한 공공 수요 {{ items|length }}건

{% for item in items -%}
■ {{ item.title }} ({{ item.institution }})
  {{ item.stage_label }}{% if item.budget %} · 예산 {{ item.budget }}{% endif %} · 입찰 예상 {{ item.window }} · 적합도 {{ item.score_pct }}%
{% if item.evidence %}  「{{ item.evidence }}」
{% endif %}  {{ item.url }}

{% endfor -%}
{% if more %}외 {{ more }}건 더 보기: {{ feed_url }}

{% endif -%}
알림 설정: {{ settings_url }}
