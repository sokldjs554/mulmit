# 실데이터 30일 적재와 측정 (2026-09-26)

첫 점검([`source-check.md`](source-check.md))은 오퍼레이션마다 첫 페이지 한 번만 불러 봤습니다. 이번에는 실제 조달청 데이터 30일치를 DB에 넣고, 파이프라인(처리 → 연결)을 끝까지 돌려 봤습니다. 이 문서의 숫자는 전부 이 실행에서 직접 잰 것이고, 계산으로 낸 값에는 따로 표시했습니다.

- 기간: 등록일 2026-08-28 ~ 2026-09-26 (30일), 발주계획·사전규격·입찰공고 × 용역·물품·공사 9개 오퍼레이션
- 환경: 개발 컨테이너의 PostgreSQL 16.13 + pgvector 0.6.0, Redis 7.0, 4 CPU. data.go.kr은 컨테이너의 프록시를 거쳐 호출했습니다.
- 경로: 워커의 `ingest_source` → `process_document` → `link_signals`와 같은 코드를 한 프로세스에서 돌리는 명령 두 개를 추가했습니다. 어댑터와 파이프라인 단계는 그대로 쓰고, 호출 예산만 덧붙였습니다.

```bash
cd apps/api
APP_DATA_GO_KR_DAILY_QUOTA=500 \
  uv run manage sources ingest -s g2b --days 30 --rows 999 --max-calls 250 --report run.json
uv run manage pipeline run       # 처리 대기 문서를 모두 처리하고 새 신호를 연결
```

`--max-calls`는 이번 실행의 서비스별 상한이고, `APP_DATA_GO_KR_DAILY_QUOTA`는 Redis에 남는 하루치 상한입니다. 재시도도 한 번으로 셉니다. 이번에는 두 번째·세 번째 적재까지 합쳐도 서비스마다 500회를 넘지 않도록 각각 250과 500으로 걸었습니다.

## 요약

- 30일치 **57,514건**(발주계획 17,707 · 사전규격 9,285 · 입찰공고 30,522)을 **91회** 호출로 받았습니다. 제공처 오류 코드는 한 번도 없었고, 재시도 3회는 모두 컨테이너 프록시의 연결 실패였습니다.
- 같은 기간을 다시 적재하니 신규 0 · 변경 0 · 건너뜀 57,514건이었습니다. 중복은 생기지 않습니다.
- 실데이터에서 버그 네 개가 나와 고쳤습니다. 가장 컸던 두 가지는 이렇습니다.
  - 기관 해석이 "서울교통공사"·"서울대학교병원"을 서울특별시로 읽고 있었습니다. 기관이 풀린 문서 6,737건 가운데 3,961건이 이런 오귀속이었습니다.
  - 유사도 연결이 서로 다른 발주계획번호를 가진 사업을 합치고 있었습니다. 확정된 유사도 연결 1,987건 가운데 1,396건이 번호와 모순됐습니다.
- 번호만으로도 단계가 꽤 이어집니다. 30일 안의 입찰공고 27,372건 가운데 73.7%가 발주계획번호를, 43.2%가 사전규격번호를 갖고 있습니다. 그런데 이어진 발주계획의 절반(50.1%)은 **입찰공고와 같은 날** 등록된 것이었습니다. 발주계획이라고 해서 늘 공고보다 먼저 오는 신호는 아닙니다.
- 파이프라인은 기관이 풀린 신호만 연결합니다. 번들 기관 사전이 데모용 37곳이라 연결까지 간 신호는 4.8%(2,776건)뿐입니다. 이게 지금 실데이터 연결에서 가장 큰 병목입니다.

## 1. 적재: 호출 수, 건수, 시간

첫 적재(07:14~07:26 UTC)의 오퍼레이션별 결과입니다. 한 번에 999건씩 받았습니다. `호출`은 페이지 수이고, `총계`는 제공처가 알려 준 `totalCount`를 주 단위 조각마다 더한 값입니다.

| 서비스 | 오퍼레이션 | 호출 | 받은 항목 | 레코드로 변환 | 총계 | 받는 데 걸린 시간 |
|---|---|---:|---:|---:|---:|---:|
| 발주계획 | `getOrderPlanSttusListServc` | 9 | 6,567 | 6,567 | 6,567 | 64.2 s |
| | `getOrderPlanSttusListThng` | 9 | 5,857 | 5,857 | 5,857 | 53.9 s |
| | `getOrderPlanSttusListCnstwk` | 9 | 5,283 | 5,283 | 5,283 | 50.7 s |
| 사전규격 | `getPublicPrcureThngInfoServc` | 9 | 4,632 | 4,632 | 4,632 | 26.4 s |
| | `getPublicPrcureThngInfoThng` | 8 | 4,448 | 4,448 | 4,448 | 20.1 s |
| | `getPublicPrcureThngInfoCnstwk` | 5 | 205 | 205 | 205 | 4.6 s |
| 입찰공고 | `getBidPblancListInfoServc` | 13 | 11,155 | 11,155 | 11,155 | 139.3 s |
| | `getBidPblancListInfoThng` | 14 | 10,934 | 10,934 | 10,934 | 127.7 s |
| | `getBidPblancListInfoCnstwk` | 12 | 8,433 | 8,433 | 8,433 | 115.2 s |

서비스별로 합치면 아래와 같습니다. `시도`는 재시도를 포함한 시도 횟수(한도 카운터에 잡히는 수)이고, `전달`은 그중 실제로 제공처까지 간 요청 수입니다.

| 서비스 | 시도 | 전달 | 재시도 | 오류(종류별) | 적재 | 전체 소요 |
|---|---:|---:|---:|---|---:|---:|
| 발주계획 | 27 | 27 | 0 | 없음 | 17,707 | 210.8 s |
| 사전규격 | 23 | 22 | 1 | `ConnectTimeout` 1 | 9,285 | 72.5 s |
| 입찰공고 | 41 | 39 | 2 | `ConnectTimeout` 2 | 30,522 | 463.0 s |

- **오류**: HTTP 오류와 제공처 오류 코드(22 한도 초과, 06~08 파라미터, 30 키 등)는 세 번의 적재 동안 한 번도 나오지 않았습니다. `ConnectTimeout`은 컨테이너 프록시가 TLS 핸드셰이크 도중 연결을 끊은 경우로, 요청이 data.go.kr에 닿기 전입니다. 같은 컨테이너에서 키 없이 `curl`로 12번 연결해 보니 5번이 이렇게 끊겼습니다. 한 번 연결되면 httpx가 연결을 재사용해서, 적재 전체로는 한 서비스에 한두 번 정도만 나왔습니다. 연결 단계 실패는 재시도해도 안전해서 이번 실행은 `APP_SOURCE_MAX_ATTEMPTS=6`으로 돌렸습니다.
- **시간**: 대부분 제공처 응답을 기다리는 시간입니다. 입찰공고 999건 한 페이지에 약 10초, 발주계획은 6~7초, 사전규격은 2~3초 걸렸습니다. DB에 넣는 시간은 입찰공고 기준 전체의 약 17%(463.0 s 중 382.2 s가 받는 시간)였습니다.
- **호출 수**: 기본값인 한 번에 100건으로 받았다면 같은 30일에 600회 가까이 필요합니다(계산: 57,514 / 100 + 조각마다 남는 페이지). 이번에는 999건씩 받아 91회로 끝났습니다. 운영 수집원에도 `sources.config`에 `{"rows": 999}`를 넣으면 같은 효과를 봅니다.
- 오늘 이 키로 쓴 호출은 Redis 카운터 기준 발주계획 82 · 사전규격 67 · 입찰공고 120회입니다(아래 두 번째·세 번째 적재 포함). 앞 세션의 점검 호출은 여기 들어 있지 않습니다.

## 2. 재실행 멱등성

같은 기간(`--until 2026-09-26`)을 곧바로 한 번 더 적재했습니다.

| 서비스 | 시도 | 받은 항목 | 신규 | 변경 | 건너뜀 |
|---|---:|---:|---:|---:|---:|
| 발주계획 | 28 | 17,707 | 0 | 0 | 17,707 |
| 사전규격 | 22 | 9,285 | 0 | 0 | 9,285 |
| 입찰공고 | 39 | 30,522 | 0 | 0 | 30,522 |

적재 뒤 `documents`는 57,514행, `(source_id, external_id)` 조합도 57,514개로 같았습니다. 문서는 이 조합으로 upsert하고 내용 해시가 같으면 건너뛰기 때문에, 창이 겹치는 매시 수집을 다시 돌려도 안전합니다. 버그를 고친 뒤 새 DB에 세 번째로 적재했을 때는 발주계획이 3건 늘어난 17,710건이었는데, 그 사이에 새로 등록된 계획들입니다.

## 3. 필드 채움과 실제 값 모양

고친 매핑으로 적재한 57,517건 기준입니다.

| 필드 | 발주계획 | 사전규격 | 입찰공고 |
|---|---:|---:|---:|
| 금액(1만 원 이상) | 97.6% | 96.7% | 98.4% |
| 발주계획번호 | 100% | 필드 없음 | 74.1% |
| 사전규격번호 | — | 100% | 43.5% |
| 공고번호 목록 (`bidNtceNoList`) | 71.8% | 56.7% | — |
| 입찰마감 (`bidClseDt`) | — | — | 92.5% |
| 계약방법 | 99.8% | 필드 없음 | 100% |
| 우리 기관 사전으로 해석된 기관 | 6.5% | 3.7% | 4.2% |

명세만 보고는 몰랐던 것들입니다.

- **금액**: 쉼표나 단위 없이 숫자만 옵니다. 그런데 0원, 1원, 10원 같은 자리표시 값이 섞여 있습니다(1만 원 미만이 입찰공고 475 · 발주계획 426 · 사전규격 300건). 대부분 단가계약이나 예산을 밝히지 않은 공고라, 예산으로 쓰면 공짜 사업처럼 랭킹·연결됩니다. 이제 1만 원 미만은 "금액 없음"으로 보고 다음 필드로 넘어갑니다. 전에는 `asignBdgtAmt`가 "0"이면 뒤의 추정가격을 보지도 않았습니다. 교복 공고의 30~46만 원은 학생 1인당 단가입니다. 총액이 아니지만 걸러 내지는 않았습니다.
- **날짜**: 등록일·공고일·마감일 모두 `YYYY-MM-DD HH:MM:SS` 한 가지 모양이었습니다. 입찰공고 2,300건(7.5%)은 마감일이 비어 있습니다.
- **번호 체계**: 발주계획 `R26DD…`, 사전규격 `R26BD…`, 입찰공고 `R26BK…`입니다. `bidNtceNoList`는 여러 개면 쉼표로 이어지는데, 발주계획은 번호 뒤에 차수 세 자리를 붙이고(`R26BK01739589000`) 사전규격은 붙이지 않습니다. 발주계획의 이 필드는 명세에서 눈여겨보지 않았던 것인데, 71.8%가 차 있어서 이번에 매핑했습니다.
- **사전규격에는 발주계획번호도 부서 필드도 없습니다.** 매핑이 읽던 `orderInsttDeptNm`는 한 건도 오지 않았습니다. `refNo`에 "회계과-39725"처럼 부서가 앞에 붙기도 하지만 모양이 제각각이라(깔끔한 "부서-번호" 모양이 2,300건) 쓰지 않았습니다.
- **공고 종류**: 등록 23,893 · 재공고 3,062 · 변경 1,829 · **취소 1,738건**입니다. 취소공고도 "공고가 나왔다"로 처리되고 있습니다(다음 단계 참고).
- **제목 머리표**: `[긴급]`처럼 대괄호 머리표는 정규화가 떼어 내지만, `(가칭)`·`(긴급)`·`(재공고)`처럼 괄호로 시작하는 머리표는 남습니다(괄호로 시작하는 제목 1,412건). `[가상입찰]` 연습 공고도 11건 있었습니다.
- **기관명**: 서로 다른 수요기관 이름이 6,344개입니다. 학교가 5,554건, 대학교 5,979건, 병원 1,382건, 공사·공단 6,591건으로, 지자체 밖 기관이 많습니다. 번들 기관 사전(데모용 37곳)으로는 그중 167개 이름, 2,776건(4.8%)만 풀립니다.
- **업종 분류**: 제목 기준 분류기가 35.7%를 `other`로 둡니다(사전규격은 44.2%). 수학여행·교복·의료소모품처럼 스마트시티 중심 분류에 없는 품목이 많고, "스포츠 페스티벌 행사 대행"이 `mobility`로 가는 식의 오분류도 보입니다. 분류 체계를 바꾸면 합성 평가 기준도 같이 바뀌어서 이번에는 기록만 했습니다.

### 기관 해석 오귀속

첫 적재에서 기관이 풀린 6,737건 가운데 3,961건(이름 330개)이 엉뚱한 기관으로 가 있었습니다.

| 원래 이름 | 해석 결과(전) | 왜 |
|---|---|---|
| 서울교통공사 · 서울대학교병원 · 분당서울대학교병원 | 서울특별시 | 이름 어딘가에 시도명이 있으면 그 시도로 봄 |
| 국토교통부 부산지방국토관리청 포항국토관리사무소 | 부산광역시 | 〃 |
| 한국농어촌공사 제주지역본부 · 경기주택도시공사 | 제주특별자치도 · 경기도 | 〃 |
| 경기도 광주시 | 경기도 | "광주시"를 광주광역시 별칭으로 보고 건너뜀 |
| 대전광역시 서구 · 부산광역시 동래구 · 인천광역시 부평구 | 유성구 · 동구 · 중구 | 퍼지 매칭이 다른 구로 붙임 |

이렇게 고쳤습니다.
- 시도 단위로 해석하려면 이름이 시도로 **시작**해야 하고, 그 뒤에 학교·공사·공단·조합이 오면 안 됩니다. 그래서 "서울특별시 영등포소방서"나 "경기도 경기도건설본부"는 여전히 시도로 풀립니다.
- "경기도 광주시"는 경기도의 시로 읽습니다.
- 퍼지 매칭은 앞머리 시도가 다르거나, 구 이름의 글자 수가 다르면 버립니다. OCR 오타("해운데구")는 글자 수가 같아서 그대로 잡힙니다.

고친 뒤 해석된 문서는 2,776건입니다. 330개 이름 가운데 시도의 기관인데 풀리지 않게 된 것은 "소방학교" 두 곳(3건)뿐이었습니다.

## 4. 단계 연결

### 번호로 이을 수 있는 만큼 (적재된 데이터에 SQL)

파이프라인과 별개로, 적재된 원자료의 번호 필드만 맞춰 본 결과입니다. 입찰공고는 차수를 빼고 공고번호 기준 27,372건입니다.

| | 건수 |
|---|---:|
| 발주계획번호가 있는 입찰공고 | 20,183 (73.7%) |
| └ 그 발주계획이 30일 안에 적재됨 | 14,040 |
| 사전규격번호가 있는 입찰공고 | 11,821 (43.2%) |
| └ 그 사전규격이 30일 안에 적재됨 | 6,129 |
| 발주계획 → 사전규격 → 입찰공고가 모두 적재됨 | 5,453 |
| 번호가 하나도 없는 입찰공고 | 6,334 (23.1%) |
| 공고번호 목록이 있는 사전규격 (모두 그 공고가 적재됨) | 5,263 |
| 공고번호 목록이 있는 발주계획 (모두 그 공고가 적재됨) | 12,707 |

- **원래 비어 있는 번호**가 있습니다. 사전규격 응답에는 발주계획번호 필드가 아예 없어서, 사전규격 ↔ 발주계획은 둘 다를 가리키는 입찰공고를 거쳐서만 이어집니다(5,453건). 공사 사전규격은 한 달에 205건뿐이라, 공사 입찰공고의 사전규격번호도 대부분 비어 있습니다.
- 발주계획의 공고번호 목록은 대부분 입찰공고의 발주계획번호를 거꾸로 적은 것입니다. 목록으로만 이어지는 계획은 7건이었습니다.
- **발주계획이 먼저 오지 않는 경우가 많습니다.** 30일 안에 이어진 발주계획 → 입찰공고 14,040쌍의 간격은 이렇습니다.

  | 간격 | 같은 날 | 1~7일 | 8~14일 | 15일 이상 | 공고보다 늦음 |
  |---|---:|---:|---:|---:|---:|
  | 건수 | 7,039 | 4,993 | 1,472 | 536 | 0 |

  이 밖에 6,143건은 30일보다 먼저 등록된 발주계획을 가리킵니다. 30일 창이라 긴 간격은 잘려서 보이지 않는 것이고, 1년치를 받아야 제대로 된 분포가 나옵니다. 사전규격 → 입찰공고 6,129쌍은 중앙값 7일, 90백분위 14일이었습니다.

### 파이프라인 연결 결과

`manage pipeline run`을 그대로 돌렸습니다. A는 버그를 고치기 전(첫 적재 그대로), B는 고친 매핑으로 새 DB에 적재하고 고친 연결 단계로 돌린 결과입니다.

| | A: 고치기 전 | B: 고친 뒤 |
|---|---:|---:|
| 처리한 문서 · 실패 | 57,514 · 0 | 57,517 · 0 |
| 신호 (문서마다 1개) | 57,514 | 57,517 |
| 연결 대상(기관이 풀린 신호) | 6,737 | 2,776 |
| 검토 대기(기관 미해석) | 50,777 | 54,741 |
| 만들어진 기회 | 2,078 | 1,580 |
| 번호로 연결 | 2,168 | 754 |
| 유사도로 연결 (확정 + 잠정) | 1,987 + 504 | 385 + 57 |
| 유사도 연결 중 번호와 모순 (확정 / 잠정) | 1,396 / 435 | 0 / 1 |
| 서로 다른 발주계획·사전규격번호가 섞인 기회 | 609 | 2 |
| 처리 시간 · 연결 시간 | 803.9 s · 90.4 s | 858.0 s · 36.8 s |

- **유사도 연결이 다른 사업을 합치고 있었습니다.** 한 기관이 "…도로 정비공사"를 한 달에 수십 건 내면, 제목·예산·시기가 비슷해 유사도가 기준을 넘습니다. 이제 이미 **다른** 발주계획번호나 사전규격번호를 가진 기회는 후보에서 뺍니다. 번호가 다르면 다른 구매이기 때문입니다. 합성 평가의 연결 P/R은 1.0 그대로입니다.
- 번호로 검증할 수 있는 유사도 연결(확정 385건 중 97건)은 모두 번호와 맞았습니다. 나머지 288건은 비교할 번호가 없어 이 방법으로는 정확도를 알 수 없습니다.
- B에서 번호 쌍이 둘 다 연결 대상이었던 경우의 재현율은 이렇습니다.
  - 입찰공고 → 발주계획: 677/677 모두 같은 기회에 들어갔습니다.
  - 입찰공고 → 사전규격: 132/141입니다. 놓친 9건은 모두, 사전규격이 먼저 기회를 따로 만든 뒤 입찰공고가 발주계획번호로 **다른** 기회에 붙은 경우입니다. 연결 단계는 이미 있는 기회 두 개를 합치지 않습니다.
- 연결 대상이 4.8%뿐인 이유는 기관 사전입니다. 연결 단계는 기관이 풀린(`accepted`) 신호만 다루는데, 번들 사전이 데모용 37곳이라 나머지는 `institution_unresolved`로 검토 대기에 갑니다. 번호 연결 자체는 기관이 없어도 되지만, 검토를 거치지 않은 신호는 연결하지 않는 게 지금 설계입니다.

## 5. 30일 규모의 핫 쿼리

B DB(문서·청크·신호 각 57,517행, `VACUUM ANALYZE` 뒤)에서 `EXPLAIN (ANALYZE, BUFFERS)`로 확인했습니다. 기회는 1,580건뿐이라 기회·추천 쪽 계획은 [`performance.md`](performance.md)의 합성 10만 건 벤치가 더 참고할 만합니다.

| 쿼리 | 계획 | 시간 | 버퍼 |
|---|---|---:|---:|
| 연결: 이미 있는 발주계획번호로 기회 찾기 | `Bitmap Index Scan (ix_signals_external_refs_gin)` → `Index Scan (opportunity_signals_signal_id_key)` | 0.14 ms | hit 8 |
| 연결: 처음 보는 발주계획번호 | 같은 계획, 0행 | 0.05 ms | hit 4 |
| 연결: 발주계획 → 이미 연결된 입찰공고 (이번에 추가) | 같은 계획 | 0.06 ms | hit 8 |
| 연결: 사전규격 공고번호 목록 | 같은 계획 | 0.04 ms | hit 4 |
| 적재: `(source_id, external_id)`로 기존 문서 찾기 | `Index Scan (uq_documents_source_external)` | 0.09 ms | hit 3, read 3 |
| 배치: 처리 대기 문서 500건 | `Index Scan (ix_documents_pending)` | 0.03 ms | hit 1 |
| 피드 첫 페이지 (점수순 21건) | `Index Scan Backward (ix_recommendations_org_score)` → `Index Scan (opportunities_pkey)` | 0.34 ms | hit 117 |

번호 연결은 모두 GIN 인덱스를 탑니다. 새로 추가한 역방향 조회(`{"bid_notice_no": …}`)도 같은 인덱스로 처리돼 인덱스를 따로 만들지 않았습니다.

## 6. 고친 것

| 문제 | 영향 (30일치) | 수정 |
|---|---|---|
| 이름에 시도명만 있어도 그 시도로 해석, "경기도 광주시" 오독, 다른 구로 퍼지 매칭 | 3,961건 오귀속 | `domain/institutions.py` |
| 1만 원 미만 자리표시 금액을 예산으로 사용, "0"이 뒤 필드를 가림 | 1,201건 | `sources/g2b.py` `_amount` |
| 발주계획의 `bidNtceNoList`(차수 포함)를 읽지 않음, 연결이 발주계획 → 입찰공고 방향으로는 찾지 않음 | 12,707건에 번호 추가 | `sources/g2b.py`, `pipeline/link.py` |
| 다른 번호를 가진 사업을 유사도로 합침 | 확정 유사도 연결 1,396건 | `pipeline/link.py` |

실제 응답 모양을 본뜬 테스트를 추가했습니다(`tests/unit/test_sources.py`, `tests/unit/test_institutions.py`, `tests/integration/test_backfill.py`). 담당자 이름·연락처는 넣지 않았고, 번호와 사업명은 바꿨습니다. `manage eval all`의 73개 지표는 고치기 전과 모두 같습니다.

## 7. 남은 문제와 다음 단계

- **기관 사전**: 가장 큰 병목입니다. 행정표준코드 전체를 들여오거나, 조달청 수요기관코드(`dminsttCd`)를 기관 식별자로 함께 써야 실데이터 연결이 제대로 돌아갑니다.
- **취소공고**: 1,738건이 "공고 나옴"으로 처리됩니다. 취소는 입찰 단계로 치지 않고, 기회를 다시 열어 두는 쪽이 맞아 보입니다.
- **기회 병합**: 입찰공고가 이미 따로 만들어진 두 기회(발주계획 쪽, 사전규격 쪽)를 번호로 가리키면 둘을 합쳐야 합니다(B에서 9건).
- **1년치 적재**: 리드타임 분포를 제대로 보려면 발주계획을 1년치 받아야 합니다. 이번 비율이면 약 21만 건이고, 오퍼레이션·주마다 999건 페이지 두 장이라 300회 남짓입니다(계산). 개발 키 하루 한도 안에 들어갑니다.
- 분류 체계를 조달 전반으로 넓히는 일, 괄호 머리표 정규화, 단가계약·1인당 단가 표시도 남았습니다.

## 다시 돌리려면

```bash
make infra
cd apps/api && uv run manage db upgrade && uv run manage seed
APP_DATA_GO_KR_DAILY_QUOTA=500 uv run manage sources ingest -s g2b --days 30 --rows 999 --max-calls 250 --report run.json
uv run manage sources ingest -s g2b --days 30 --until <같은 날짜> --rows 999 --max-calls 250   # 멱등성
uv run manage pipeline run
```

적재는 수집원마다 끝날 때 커밋합니다. 한도나 오류를 만나면 그 수집원만 멈추고 받은 것까지는 남기며, 다음 수집원으로 넘어갑니다. 이때 종료 코드는 1입니다.
